import React, { Fragment } from 'react'
import PropTypes from 'prop-types'
import { ItemsMenu } from './items-menu'


export const Items = ({}) =>
	<Fragment>
		<ItemsMenu />
	</Fragment>

Items.propTypes = {
	
}